eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
    register-with-eureka: false   #不向自己注册
  server:
    enable-self-preservation: false   #检查 只能在开发环境关闭   生产环境要打开
server:
  port: 8090
spring:
  application:
    name: eureka
